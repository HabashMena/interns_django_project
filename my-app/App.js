import "./App.css"; // Importing the CSS file to apply styles to the app
import TicTacToe from "./components/TicTacToe"; // Import the TicTacToe component that contains the main game logic

// Main component for the app
function App() {
  return <TicTacToe />; // Rendering the TicTacToe component within the App
}

export default App; // Exporting App component so it can be used in other files
